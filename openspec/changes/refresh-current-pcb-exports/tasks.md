# Tasks

- [ ] Validate this export-only proposal.
- [ ] Export the current PCB SVG to `.copperhead/renders/`.
- [ ] Refresh `outputs/board.svg` and permitted deterministic bundled outputs using the export tool when available.
- [ ] Run ERC and DRC.
- [ ] Run schematic legibility, drift, and constraint/spec validation.
- [ ] Confirm exports derive from the current committed PCB and finish only when all checks are clean.
